/**
 * User-visible app name in chrome (e.g. top bar). Replaced when scaffolding: TemplateBareApp → MyApp.
 * Keep aligned with `app.json` `displayName` / native display name.
 */
export const APP_DISPLAY_NAME = 'TemplateBareApp';
