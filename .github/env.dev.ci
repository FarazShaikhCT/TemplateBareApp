# Committed CI/local fallback for dev flavor builds when `.env.dev` is absent (gitignored).
# JSONPlaceholder serves /posts for PostsScreen FlatList FPS testing.
API_BASE_URL=https://jsonplaceholder.typicode.com
APP_ENV=development
